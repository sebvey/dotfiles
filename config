

Host sve.gitlab.com
    HostName gitlab.com
    IdentityFile ~/.ssh/id_ed25519_sebvey


# When not described host, default identity is used
# no need to precise it (even more, it conflicts with ssh-agent)
# Host *
#     IdentityFile ~/.ssh/id_ed25519

# Host alias.gitlab.com # alias.gitlab.com to use instead of gitlab.com in ssh or gitlab
#     IgnoreUnknown UseKeychain # will ignore UseKeyChain on Linux
#     UseKeychain yes # MacOs Specific, will store once for all the passphrase with KeyChain
#     AddKeysToAgent yes # key added to the ssh-agent
